---
import { sucursales } from '../data/site';
import { idiomaDe } from '../i18n/idiomas';
import { textos } from '../i18n/textos';
import TituloSeccion from './TituloSeccion.astro';
import TarjetaSucursal from './TarjetaSucursal.astro';

const idioma = idiomaDe(Astro.currentLocale);
const T = textos(idioma);
---

<section
  id="sucursales"
  class="border-fondo-200 bg-pared-100 border-t py-24 md:py-32 lg:py-16"
>
  <div class="mx-auto max-w-6xl px-5">
    <TituloSeccion
      etiqueta={T.sucursalesEtiqueta}
      titulo={T.sucursalesTitulo}
      descripcion={T.sucursalesDescripcion}
    />

    {
      /* Las dos sucursales una al lado de otra: son una eleccion, no una lista,
        y comparadas de un vistazo se elige antes. */
    }
    <div class="mt-14 grid gap-6 lg:mt-8 lg:grid-cols-2">
      {sucursales.map((sucursal) => <TarjetaSucursal sucursal={sucursal} compacta />)}
    </div>
  </div>
</section>
